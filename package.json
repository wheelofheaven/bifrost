{
  "name": "bifrost-theme",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "bundle": "node scripts/bundle.js",
    "bundle:watch": "node scripts/bundle.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
