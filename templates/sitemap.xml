<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
        xmlns:image="http://www.google.com/schemas/sitemap-image/1.1">
{# Zola sitemap template - entries is provided by Zola with all pages and sections #}
{# Skip noindexed pages and generated source-record stubs (extra.source_id):
   keeping them out of the sitemap stops them diluting crawl budget and avoids
   advertising URLs we mark noindex. Mirrors the robots logic in partials/seo.html. #}
{% for sitemap_entry in entries %}
{% if not sitemap_entry.extra.noindex and not sitemap_entry.extra.source_id %}
    <url>
        <loc>{{ sitemap_entry.permalink | safe }}</loc>
        {% if sitemap_entry.date %}
        <lastmod>{{ sitemap_entry.date | date(format="%Y-%m-%d") }}</lastmod>
        {% endif %}
        <changefreq>{% if sitemap_entry.permalink is containing('/wiki/') %}monthly{% elif sitemap_entry.permalink is containing('/timeline/') %}monthly{% elif sitemap_entry.permalink is containing('/articles/') %}weekly{% elif sitemap_entry.permalink is containing('/read/') %}monthly{% else %}weekly{% endif %}</changefreq>
        <priority>{% if sitemap_entry.permalink is matching("^https?://[^/]+/?$") %}1.0{% elif sitemap_entry.permalink is containing('/read/') %}0.9{% elif sitemap_entry.permalink is containing('/wiki/') %}0.7{% elif sitemap_entry.permalink is containing('/timeline/') %}0.7{% elif sitemap_entry.permalink is containing('/library/') %}0.7{% elif sitemap_entry.permalink is containing('/articles/') %}0.6{% else %}0.5{% endif %}</priority>
        {#- Add image if available in extra data -#}
        {%- if sitemap_entry.extra.image and sitemap_entry.extra.image is containing("/") -%}
        <image:image>
            <image:loc>{% if sitemap_entry.extra.image is starting_with("http") %}{{ sitemap_entry.extra.image | safe }}{% else %}{{ get_url(path=sitemap_entry.extra.image) | safe }}{% endif %}</image:loc>
            {%- if sitemap_entry.extra.image_alt -%}
            <image:caption>{{ sitemap_entry.extra.image_alt | escape }}</image:caption>
            {%- endif -%}
        </image:image>
        {%- endif -%}
    </url>
{% endif %}
{% endfor %}
</urlset>
